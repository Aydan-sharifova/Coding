import Editor, { type OnMount } from "@monaco-editor/react";
import "../features/editor/monacoSetup";
import type { editor } from "monaco-editor";
import { useCallback, useEffect, useRef, useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { useNavigate, useParams } from "react-router-dom";
import { ConfirmDialog, Dialog } from "../components/ui/Dialog";
import { useToast } from "../contexts/ToastContext";
import { useTheme } from "../hooks/useTheme";
import { useEditorStore, type EditorTab } from "../features/editor/editorStore";
import { useAutoSave } from "../features/editor/hooks/useAutoSave";
import { useEditorTabs } from "../features/editor/hooks/useEditorTabs";
import { useKeyboardShortcuts } from "../features/editor/hooks/useKeyboardShortcuts";
import { getOrCreateModel, useMonacoConfiguration } from "../features/editor/hooks/useMonacoConfiguration";
import { fileExplorerApi } from "../features/fileExplorer/api";
import { FileTree } from "../features/fileExplorer/FileTree";
import { explorerStore, useExplorerSnapshot } from "../features/fileExplorer/store";
import type { WorkspaceNode } from "../features/fileExplorer/types";
import { VersionHistory } from "../features/fileExplorer/VersionHistory";
import { signalRService } from "../features/collaboration/signalRService";
import { useCollaboration } from "../features/collaboration/useCollaboration";
import { PresencePanel } from "../features/collaboration/PresencePanel";
import { useCollaborationStore } from "../features/collaboration/collaborationStore";
import { useCodingSession } from "../features/analytics/useCodingSession";

function MonacoPane({ tab }: { tab: EditorTab }) {
  const { theme } = useTheme(); const fontSize = useEditorStore((state) => state.fontSize); const updateContent = useEditorStore((state) => state.updateContent); const setCursor = useEditorStore((state) => state.setCursor); const setViewState = useEditorStore((state) => state.setViewState); const remoteCursors = useCollaborationStore((state) => state.remoteCursors); const { configure } = useMonacoConfiguration(); const editorRef = useRef<editor.IStandaloneCodeEditor | undefined>(undefined); const decorations = useRef<editor.IEditorDecorationsCollection | undefined>(undefined); const typingTimer = useRef<number | undefined>(undefined);
  const mount: OnMount = (instance, monaco) => { editorRef.current = instance; const model = getOrCreateModel(monaco, tab.id, tab.path, tab.content, tab.language); instance.setModel(model); if (tab.viewState) instance.restoreViewState(tab.viewState); instance.focus(); instance.onDidChangeCursorPosition((event) => { setCursor(tab.id, event.position.lineNumber, event.position.column); const selected = instance.getSelection(); signalRService.updateCursor({ fileId: tab.id, lineNumber: event.position.lineNumber, column: event.position.column, selection: selected ? { startLineNumber: selected.startLineNumber, startColumn: selected.startColumn, endLineNumber: selected.endLineNumber, endColumn: selected.endColumn } : undefined }); }); instance.onDidChangeModelContent((event) => { if (event.isFlush) return; signalRService.startTyping(tab.id); if (typingTimer.current) window.clearTimeout(typingTimer.current); typingTimer.current = window.setTimeout(() => signalRService.stopTyping(tab.id), 900); event.changes.forEach((change) => signalRService.sendOperation({ operationId: crypto.randomUUID(), fileId: tab.id, range: { startLineNumber: change.range.startLineNumber, startColumn: change.range.startColumn, endLineNumber: change.range.endLineNumber, endColumn: change.range.endColumn }, insertedText: change.text, deletedLength: change.rangeLength })); }); };
  useEffect(() => { const instance = editorRef.current; if (!instance) return; decorations.current?.clear(); decorations.current = instance.createDecorationsCollection(Object.values(remoteCursors).filter((cursor) => cursor.fileId === tab.id).flatMap((cursor) => { const result: editor.IModelDeltaDecoration[] = [{ range: { startLineNumber: cursor.lineNumber, startColumn: cursor.column, endLineNumber: cursor.lineNumber, endColumn: cursor.column }, options: { className: "remote-cursor" } }]; if (cursor.selection) result.push({ range: cursor.selection, options: { className: "remote-selection" } }); return result; })); }, [remoteCursors, tab.id]);
  useEffect(() => () => { if (typingTimer.current) window.clearTimeout(typingTimer.current); signalRService.stopTyping(tab.id); decorations.current?.clear(); if (editorRef.current) setViewState(tab.id, editorRef.current.saveViewState()); }, [setViewState, tab.id]);
  return <Editor key={tab.id} height="100%" path={tab.path} language={tab.language} value={tab.content} theme={theme === "dark" ? "coding-dark" : "coding-light"} beforeMount={configure} onMount={mount} onChange={(value) => updateContent(tab.id, value ?? "")} keepCurrentModel options={{ fontSize, fontLigatures: true, minimap: { enabled: true }, smoothScrolling: true, automaticLayout: true, scrollBeyondLastLine: false, tabSize: 2, wordWrap: "off", padding: { top: 12 } }} />;
}

export function FileExplorerPage() {
  const { projectId = "" } = useParams(); const navigate = useNavigate(); const { show } = useToast(); const explorer = useExplorerSnapshot(); const tabs = useEditorTabs(); const { saveNow } = useAutoSave();
  const collaboration = useCollaboration(projectId, tabs.activeTabId);
  useCodingSession(projectId, tabs.activeTabId);
  const tree = useQuery({ queryKey: ["file-tree", projectId], queryFn: () => fileExplorerApi.tree(projectId), enabled: Boolean(projectId) });
  const loadedTree = useRef<WorkspaceNode[] | undefined>(undefined);
  const [create, setCreate] = useState<{ type: "file" | "folder"; parentId?: string }>(); const [newName, setNewName] = useState(""); const [deleting, setDeleting] = useState<WorkspaceNode>(); const [closing, setClosing] = useState<EditorTab>(); const [quickOpen, setQuickOpen] = useState(false); const [quickFilter, setQuickFilter] = useState(""); const [historyKey, setHistoryKey] = useState(0);
  useEffect(() => { if (tree.data && loadedTree.current !== tree.data) { loadedTree.current = tree.data; explorerStore.load(tree.data); } }, [tree.data]);
  const reloadTree = async () => { const result = await tree.refetch(); if (result.data) explorerStore.load(result.data); };
  const requestClose = useCallback((id?: string) => { if (!id) return; const tab = useEditorStore.getState().tabs[id]; if (!tab) return; if (tab.content !== tab.savedContent) setClosing(tab); else useEditorStore.getState().closeTab(id); }, []);
  useKeyboardShortcuts({ save: () => { if (tabs.activeTabId) void saveNow(tabs.activeTabId).catch((error) => show(error instanceof Error ? error.message : "Save failed.", "error")); }, close: () => requestClose(tabs.activeTabId), quickOpen: () => setQuickOpen(true) });
  const createNode = async () => { if (!create || !newName.trim()) return; try { create.type === "file" ? await fileExplorerApi.createFile(projectId, create.parentId, newName) : await fileExplorerApi.createFolder(projectId, create.parentId, newName); setCreate(undefined); setNewName(""); await reloadTree(); show(`${create.type === "file" ? "File" : "Folder"} created.`); } catch (error) { show(error instanceof Error ? error.message : "Creation failed.", "error"); } };
  const actions = { onOpen: (node: WorkspaceNode) => void tabs.openFile(node).catch((error) => show(error instanceof Error ? error.message : "Unable to open file.", "error")), onCreate: (type: "file" | "folder", parentId?: string) => { setCreate({ type, parentId }); setNewName(""); }, onRename: async (node: WorkspaceNode, name: string) => { try { await fileExplorerApi.rename(node.id, name); await reloadTree(); } catch (error) { show(error instanceof Error ? error.message : "Rename failed.", "error"); } }, onDelete: setDeleting, onMove: async (nodeId: string, parentId?: string) => { if (!nodeId) return; try { await fileExplorerApi.move(nodeId, parentId); await reloadTree(); } catch (error) { show(error instanceof Error ? error.message : "Move failed.", "error"); } } };
  const resize = (side: "left" | "right", start: number) => { const initialLeft = tabs.leftWidth; const initialRight = tabs.rightWidth; const move = (event: MouseEvent) => tabs.setPanelWidths(side === "left" ? Math.min(480, Math.max(180, initialLeft + event.clientX - start)) : initialLeft, side === "right" ? Math.min(480, Math.max(220, initialRight - event.clientX + start)) : initialRight); const stop = () => { window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", stop); }; window.addEventListener("mousemove", move); window.addEventListener("mouseup", stop); };
  const files = [...explorer.entities.values()].filter((node) => node.nodeType === "File" && `${node.name} ${node.path}`.toLowerCase().includes(quickFilter.toLowerCase()));
  return <main className="workspace-page"><header className="workspace-toolbar"><button onClick={() => navigate(`/projects/${projectId}/settings`)}>← Project</button><strong>Collaborative Workspace</strong><div><button onClick={() => setQuickOpen(true)}>⌘P</button><button onClick={() => actions.onCreate("file", explorer.selectedId)}>＋ File</button><button onClick={() => actions.onCreate("folder", explorer.selectedId)}>＋ Folder</button><button onClick={tabs.toggleRightPanel}>Collaboration / History</button></div></header><div className="monaco-workspace" style={{ gridTemplateColumns: `${tabs.leftWidth}px 4px minmax(280px, 1fr) ${tabs.rightPanelVisible ? `4px ${tabs.rightWidth}px` : ""}` }}><aside className="explorer-panel"><header><span>EXPLORER</span><button onClick={() => reloadTree()}>↻</button></header>{tree.isLoading ? <div className="tree-empty">Loading files…</div> : <FileTree {...actions} />}</aside><button className="panel-resizer" aria-label="Resize explorer" onMouseDown={(event) => resize("left", event.clientX)} /><section className="editor-shell"><div className="editor-tabs" role="tablist">{tabs.openTabIds.map((id) => { const tab = tabs.tabs[id]; const dirty = tab.content !== tab.savedContent; return <button key={id} role="tab" aria-selected={id === tabs.activeTabId} className={id === tabs.activeTabId ? "active" : ""} onClick={() => tabs.activateTab(id)}><span>{tab.name}</span>{dirty && <i title="Unsaved changes">●</i>}<b onClick={(event) => { event.stopPropagation(); requestClose(id); }}>×</b></button>; })}</div><div className="monaco-host">{tabs.activeTab ? <MonacoPane tab={tabs.activeTab} /> : <div className="editor-empty"><span className="brand-mark">C</span><h2>Select a file to begin</h2><p>Double-click a file or press Ctrl/Cmd+P.</p></div>}</div><footer className="editor-statusbar"><span>{tabs.activeTab?.status ?? "Ready"} · {collaboration.connectionState}</span><span>{tabs.activeTab ? `Ln ${tabs.activeTab.cursor.lineNumber}, Col ${tabs.activeTab.cursor.column}` : ""}</span><span>{tabs.activeTab?.language ?? "Plain Text"}</span><label>Font <input type="number" min="10" max="28" value={tabs.fontSize} onChange={(event) => tabs.setFontSize(Number(event.target.value))} /></label></footer></section>{tabs.rightPanelVisible && <><button className="panel-resizer" aria-label="Resize collaboration panel" onMouseDown={(event) => resize("right", event.clientX)} /><aside className="collaboration-panel"><PresencePanel />{tabs.activeTab ? <VersionHistory nodeId={tabs.activeTab.id} refreshKey={historyKey} onRestore={async () => { const fresh = await fileExplorerApi.content(tabs.activeTab!.id); tabs.acceptExternal(fresh.nodeId, fresh.content, fresh.concurrencyToken); setHistoryKey((key) => key + 1); show("Version restored."); }} /> : <div className="tree-empty">Open a file to view collaboration and history.</div>}</aside></>}</div><Dialog open={Boolean(create)} title={`New ${create?.type ?? "node"}`} onClose={() => setCreate(undefined)} footer={<><button className="ui-button ghost" onClick={() => setCreate(undefined)}>Cancel</button><button className="ui-button primary" onClick={createNode}>Create</button></>}><div className="feature-form"><label>Name<input autoFocus value={newName} onChange={(event) => setNewName(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") void createNode(); }} /></label></div></Dialog><ConfirmDialog open={Boolean(deleting)} title={`Delete ${deleting?.name ?? "node"}?`} description={deleting?.nodeType === "Folder" ? "This folder and every descendant will be soft-deleted." : "The file and history will be hidden."} destructive confirmLabel="Delete" onClose={() => setDeleting(undefined)} onConfirm={async () => { if (!deleting) return; try { await fileExplorerApi.remove(deleting.id); if (tabs.tabs[deleting.id]) tabs.closeTab(deleting.id); setDeleting(undefined); await reloadTree(); } catch (error) { show(error instanceof Error ? error.message : "Delete failed.", "error"); } }} /><Dialog open={Boolean(closing)} title={`Save changes to ${closing?.name}?`} description="Your changes will be lost if you close without saving." onClose={() => setClosing(undefined)} footer={<><button className="ui-button ghost" onClick={() => setClosing(undefined)}>Cancel</button><button className="ui-button danger" onClick={() => { if (closing) { tabs.discardChanges(closing.id); tabs.closeTab(closing.id); } setClosing(undefined); }}>Don't Save</button><button className="ui-button primary" onClick={async () => { if (!closing) return; try { await saveNow(closing.id); tabs.closeTab(closing.id); setClosing(undefined); } catch (error) { show(error instanceof Error ? error.message : "Save failed.", "error"); } }}>Save</button></>}><div className="confirmation-note">The server concurrency token will be checked before saving.</div></Dialog><Dialog open={quickOpen} title="Quick Open" onClose={() => setQuickOpen(false)}><div className="quick-open"><input autoFocus value={quickFilter} onChange={(event) => setQuickFilter(event.target.value)} placeholder="Search files by name or path…" />{files.slice(0, 20).map((node) => <button key={node.id} onClick={() => { void tabs.openFile(node); setQuickOpen(false); setQuickFilter(""); }}><strong>{node.name}</strong><small>{node.path}</small></button>)}</div></Dialog></main>;
}
